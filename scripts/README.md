# Scripts

Shared repository scripts live at this level.

- `mochi/`: Mochi local SQLite cleanup and verification scripts used by
  `make mochi.*` targets.
- `publish.mjs`, `generate-openapi-types.mjs`, and other root-level scripts are
  cross-project tooling retained at the top of this folder.
